\newcommand{\deplisting}[4]{
    \pbodyitem{
        \href{#2}{#1}
    }
    {
        {#3}
        \textbf{Project Locations Used:}
        \begin{itemize}
            {#4}
        \end{itemize}
    }
}



\section{Dependencies}
This section details all the external dependencies the project utilizes.
Each dependency is enumerated, along with a justification for why they are used, and a listing of subsystems and modules in which they are used.

\begin{itemize}
    \deplisting{egui/eframe}
    {https://www.egui.rs/}
    {}
    {
        \pbodyitem
        {UI}
        {
            The UI subsystem uses the egui library via the eframe framework to display the user interface in either a browser or as a native desktop application.
        }
    }

    % \deplisting{web_sys}
    % {https://rustwasm.github.io/wasm-bindgen/web-sys/index.html}
    % {}
    % {
    %     \pbodyitem{test}{test}
    % }

    % \deplisting{sqlite}
    % {https://github.com/stainless-steel/sqlite}
    % {}
    % {
    %     \pbodyitem{test}{test}
    % }

    % \deplisting{lyon}
    % {https://github.com/nical/lyon}
    % {}
    % {
    %     \pbodyitem{test}{test}
    % }

    % \deplisting{Roopes}
    % {https://docs.rs/roopes/latest/roopes/}
    % {}
    % {
    %     \pbodyitem{test}{test}
    % }
\end{itemize}